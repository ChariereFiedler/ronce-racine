# Generic rules managed by the Ronce Racine canonical source.
# One per line. Rules NOT listed are left untouched (locally customized versions).
# Sync: npx tsx /path/to/ronce-racine/install.ts install . --rules-only
minimal-code.md
commits.md
test-discipline.md
# secure-logging.md          # ← uncomment to adopt the generic version
# detection-gap-protocol.md  # ← same

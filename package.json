{
  "name": "ronce-racine",
  "version": "0.4.0",
  "private": true,
  "type": "module",
  "description": "Ronce Racine - generic Claude Code config (always-on rules + on-demand skills) with a distribution CLI",
  "license": "MIT",
  "bin": { "ronce-racine": "./install.ts" },
  "engines": { "node": ">=18" },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "tsx tools/skills.ts validate && tsx tools/skills.ts triggers && tsx tools/skills.ts rules && tsx tools/skills.ts scripts && tsx tools/skills.ts hooks && tsx tools/skills.ts templates && tsx tools/tests.ts",
    "test:mutation": "tsx tools/mutations.ts",
    "eval:dry": "tsx playground/setup.ts >/dev/null && tsx tools/eval.ts run --dry-run",
    "skills:list": "tsx tools/skills.ts list",
    "skills:triggers": "tsx tools/skills.ts triggers",
    "rules:validate": "tsx tools/skills.ts rules"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.0.0",
    "typescript": "^5.0.0"
  }
}
